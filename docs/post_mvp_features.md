# Post-MVP ideas

For **immediate engineering / content checklist** (Strapi migration, linking, deploy), use **[NEXT_STEPS.md](./NEXT_STEPS.md)**.

---

## Export / Notion / tracker (parked)

Idea: export a hub page or directory into CSV / Notion so students can turn it into a personal tracker.

**Assessment:** CSV export is straightforward technically; whether it ships depends on whether the core browse + external directory experience is solid first. Park until post-MVP unless demand is clear.
