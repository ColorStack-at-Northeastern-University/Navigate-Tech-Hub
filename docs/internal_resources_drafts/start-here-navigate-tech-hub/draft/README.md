# Start Here — anchor guide (editorial)

This folder is **not** in `INTERNAL_GUIDE_INVENTORY.md` on purpose: it is the one-off homepage anchor you write in your own voice.

After editing `article.md`, create or update the Strapi **Resource** entry using `STRAPI.md` in the parent folder, then set **`startHere: true`** (only one guide should have this flag).
